# coding=utf-8

#round(a, 4)  # 保留四位小数
#divmod(a, b)   # a/b的整除与余数同时进行得到一个二元元组
#pow(a, b, c)   #a**b%c

#bin(4)     # 10进制转换为2进制
#oct(4)     # 10进制转换为8进制
#hex(f)     # 10进制转换为16进制

#chr(x)     # 编码-->单字符
#ord(x)     # 单字符-->编码

#"{:>15}.format(s)"     # >右对齐，<左对齐，^居中，数字为宽度
#"{:*>15}.format(s)"    # 空余用*填充

###集合操作
# S&T 交集    S|T 并集      S^T 补集(非共同元素)      S-T 差集
# 方法:add(x) remove(x) clear()


# ','.join(list)
